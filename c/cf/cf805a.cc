#include <iostream>
#include <cmath>
/* CodeForces CF805A problem */
using namespace std;

int main(int argc, char **argv) {
	int64_t l, r;
	cin >> l;
	cin >> r;
	if (l == r)
		cout << l;
	else
		cout << 2;
	cout << endl;
	return 0;
}
